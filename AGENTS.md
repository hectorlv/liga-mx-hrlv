# AGENTS
- Para compilar el proyecto usa `corepack pnpm run build`.
- Si el servidor no está levantado, usa `corepack pnpm run start:build:open` para compilar, levantar el servidor y poder abrir en el navegador.
- Si el servidor ya está levantado, reutilízalo: recompila y actualiza el navegador cuando el build termine correctamente. Esto describe la actualización del servidor, no la validación completa del cambio. Verifica el comportamiento afectado con las comprobaciones pertinentes. Termina cuando el alcance solicitado esté implementado y verificado, o informa con precisión qué parte permanece bloqueada y por qué.
