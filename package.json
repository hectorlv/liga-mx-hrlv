{
  "name": "liga-mx-hrlv",
  "description": "Web component liga-mx-hrlv following open-wc recommendations",
  "license": "MIT",
  "author": "liga-mx-hrlv",
  "version": "3.0.1",
  "type": "module",
  "scripts": {
    "lint": "eslint --ext .js,.html . --ignore-path .gitignore && prettier \"**/*.js\" --check --ignore-path .gitignore",
    "format": "eslint --ext .js,.html . --fix --ignore-path .gitignore && prettier \"**/*.js\" --write --ignore-path .gitignore",
    "build": "rimraf dist && rollup -c rollup.config.js --bundleConfigAsCjs && npm run analyze -- --exclude dist",
    "start:build:open": "npm run build && web-dev-server --root-dir dist --app-index index.html --open",
    "start:build": "npm run build && web-dev-server --root-dir dist --app-index index.html",
    "analyze": "cem analyze --litelement",
    "start": "web-dev-server"
  },
  "dependencies": {
    "@material/web": "^2.4.1",
    "@polymer/iron-icon": "^3.0.1",
    "@polymer/iron-icons": "^3.0.1",
    "firebase": "^12.5.0",
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.5",
    "@custom-elements-manifest/analyzer": "^0.11.0",
    "@open-wc/eslint-config": "^13.0.0",
    "@rollup/plugin-babel": "^6.1.0",
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@web/dev-server": "^0.4.6",
    "@web/rollup-plugin-html": "^2.3.0",
    "@web/rollup-plugin-import-meta-assets": "^2.3.0",
    "babel-plugin-template-html-minifier": "^4.1.0",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.6.2",
    "rimraf": "^6.1.0",
    "rollup": "^4.53.2",
    "rollup-plugin-esbuild": "^6.2.1",
    "rollup-plugin-image": "^1.0.2",
    "rollup-plugin-workbox": "^8.1.2"
  },
  "eslintConfig": {
    "extends": [
      "@open-wc",
      "prettier"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "arrowParens": "avoid"
  },
  "customElements": "custom-elements.json"
}
